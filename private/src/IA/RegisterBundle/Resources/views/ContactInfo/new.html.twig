{% extends '::base.html.twig' %}

{% form_theme form _self %}

{% if form is defined %}
    {% form_theme form 'IARegisterBundle:Form:fields.html.twig' %}
{% endif %}

{% block body -%}
    <div class="span7">
        <h1>Contact Information creation</h1>

        <form action="{{ path('contact_info_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
            {{ form_widget(form) }}
            <p>
                <button type="submit" class="btn btn-primary pull-left">Create</button>
            </p>
        </form>

        <div class="record_actions">
            <a href="{{ path('contact_info') }}">
                Back to the list
            </a>
        </div>
    </div>
{% endblock %}
