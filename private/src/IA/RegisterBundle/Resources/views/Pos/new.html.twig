{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pos creation</h1>

    <form action="{{ path('pos_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('user_new') }}">
                Create new user
            </a>
        </li>
        <li>
            <a href="{{ path('pos') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
