{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pos</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Organization_id</th>
                <td>{{ entity.organizationid }}</td>
            </tr>
            <tr>
                <th>Contact_info_id</th>
                <td>{{ entity.contactinfoid }}</td>
            </tr>
            <tr>
                <th>Country_id</th>
                <td>{{ entity.countryid }}</td>
            </tr>
            <tr>
                <th>City_id</th>
                <td>{{ entity.cityid }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ entity.region }}</td>
            </tr>
            <tr>
                <th>Data_json</th>
                <td>{{ entity.datajson }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ entity.updatedat|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ entity.createdat|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pos') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pos_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('pos_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
